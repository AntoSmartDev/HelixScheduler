@page
@model InstructionsModel
@{
    ViewData["Title"] = "Instructions";
}

<h1 class="h3 mb-3">Sample usage</h1>

<div class="card shadow-sm mb-3">
    <div class="card-body">
        <h2 class="h5">Run the API</h2>
        <p class="mb-0">Start the HelixScheduler.WebApi project and confirm it listens on the URL configured in <code>AvailabilityApi:BaseUrl</code>.</p>
    </div>
</div>

<div class="card shadow-sm mb-3">
    <div class="card-body">
        <h2 class="h5">Run this sample</h2>
        <p class="mb-0">Start HelixScheduler.Samples.Medical and open the home page to search availability.</p>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <h2 class="h5">Manual API check</h2>
        <p class="mb-0">Compare the UI output with a direct call to <code>/api/availability/slots</code> using the same date and resources.</p>
    </div>
</div>

<div class="card shadow-sm mt-3">
    <div class="card-body">
        <h2 class="h5">Suggested demo range</h2>
        <p class="mb-0">Use 2026-03-09 to 2026-03-12 with Doctor 8 + Room 2 for seeded availability.</p>
    </div>
</div>

<div class="card shadow-sm mt-3">
    <div class="card-body">
        <h2 class="h5">Resource list</h2>
        <p class="mb-0">The sample loads resources from <code>/api/resources?onlySchedulable=true</code>.</p>
    </div>
</div>
