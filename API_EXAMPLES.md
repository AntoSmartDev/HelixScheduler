# API Examples

Canonical API examples now live in [docs/api-examples.md](docs/api-examples.md).

This root file is kept only as a compatibility pointer to avoid stale duplicate examples.
Use the `docs/` version for the current availability and catalog HTTP examples.
